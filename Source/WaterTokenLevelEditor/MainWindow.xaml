<Window x:Name="win_mainWindow" x:Class="WaterTokenLevelEditor.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Water Token Level Editor" Height="350" Width="525" MinWidth="525" MinHeight="350">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="0.15*" MinWidth="120"/>
            <ColumnDefinition Width="0.7*" MinWidth="120"/>
            <ColumnDefinition Width="0.15*" MinWidth="120"/>
        </Grid.ColumnDefinitions>
        
        <Grid.RowDefinitions>
            <RowDefinition Height="20" MaxHeight="20" MinHeight="20" />
            <RowDefinition Height="*"  MinHeight="120" />
            <RowDefinition Height="26" MaxHeight="26" MinHeight="26"/>
        </Grid.RowDefinitions>


        <Menu Grid.ColumnSpan ="3" Grid.Row="0" x:Name="men_mainMenu" Height="20" Margin="0" VerticalAlignment="Top" BorderBrush="{DynamicResource {x:Static SystemColors.ControlDarkDarkBrushKey}}" BorderThickness="0,0,0,1" MaxHeight="20" MinHeight="20" Background="#FFF0F0F0">
            
            <MenuItem Header="_File" >
                <MenuItem Header="_New" />
                <MenuItem Header="_Load" />
                <MenuItem Header="_Save" />
            </MenuItem>

            <MenuItem Header="_Layout">
                <MenuItem Header="_GridSize" />
            </MenuItem>

            <MenuItem Header="_Help">
                <MenuItem Header="_About" />
            </MenuItem>
            
        </Menu>

        <DockPanel Grid.Column="0" Grid.Row="1" HorizontalAlignment="Stretch" LastChildFill="False" Margin="0" VerticalAlignment="Stretch" Background="Red"/>

        <DockPanel Grid.Column="1" Grid.Row="1" HorizontalAlignment="Stretch" LastChildFill="False" Margin="0" VerticalAlignment="Stretch" Background="Green"/>

        <DockPanel Grid.Column="2" Grid.Row="1" HorizontalAlignment="Stretch" LastChildFill="False" Margin="0" VerticalAlignment="Stretch" Background="Blue"/>


        <Label Grid.ColumnSpan ="3" Grid.Row="2" x:Name="lbl_statusLabel" Content="Label" Height="26" Margin="0" VerticalAlignment="Bottom" BorderThickness="0,1,0,0" Background="{DynamicResource {x:Static SystemColors.ControlLightBrushKey}}" BorderBrush="{DynamicResource {x:Static SystemColors.ControlDarkDarkBrushKey}}" MinHeight="26" MaxHeight="26"/>

    </Grid>
</Window>
